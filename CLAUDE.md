# Project rules

## Writing / content
- Never use em-dashes (—) anywhere: page copy, headings, alt text, meta tags, comments, commit messages, or chat replies. Use a comma, colon, or period (or restructure the sentence) instead.
- En-dashes (–) are allowed only for numeric ranges (e.g. "40–50%", "+10–15%").
