# about.html — UI Spec

<!-- AUTO:START — regenerated by `npm run spec`. Do not edit below. -->

### Tokens (overrides vs `_tokens.md`)

- `--accent-dark`: #001340
- `--section-gap`: clamp(72px, 9vw, 112px)

### Section inventory

1. **hero** — H: "We Built the AI First. Then We Made It Available to You."
2. **story** — H: "Our Story"
3. **mission**
4. **different** — H: "What Makes Us Different"
5. **cta** — H: "Ready to Embed the AI That's Already Live?"
6. **lead-section** `#lead-form` — H: "Book a demo and watch your job get done by Infina AI"

<!-- AUTO:END -->

## Nx mapping (manual)

**Route:** `/about` · **Page:** `src/pages/landing/LandingAbout.tsx` · **CSS:** `src/styles/landing-about.css`

| HTML section | Nx component (`…/components/landing/pages/about/…`) | Notes |
|--------------|-----------------------------------------------------|-------|
| 1. hero | `AboutHero.tsx` | |
| 2. story | `AboutStorySection.tsx` | |
| 3. mission | `AboutMissionSection.tsx` | |
| 4. different | `AboutDifferentSection.tsx` | |
| 5. cta | `AboutCtaSection.tsx` | |
| 6. lead-section `#lead-form` | `sections/LeadSection.tsx` | shared (note `#lead-form` id, not `#demo`) |

## Design intent & interactions (manual)

- Lightest page: ~9 keyframes, 8 clip-path. No scroll observers/rAF. Mostly static content with gradient-text accents.
