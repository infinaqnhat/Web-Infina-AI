# focus-alignment.html — UI Spec

<!-- AUTO:START — regenerated by `npm run spec`. Do not edit below. -->

### Tokens (overrides vs `_tokens.md`)

- `--fg`: #1a1f2e
- `--fg-secondary`: #334155
- `--muted`: #64748b
- `--accent-dark`: #001340
- `--blue`: #1863dc
- `--blue-light`: rgba(24, 99, 220, 0.08)
- `--section-gap`: 96px
- `--radius`: 20px

### Section inventory

1. **hero** — H: "The AI chief of staff for teams under 50."
2. **section** — H: "AI that understands your context"
3. **section** — H: "Built for the whole team not just the manager."
4. **section** — H: "Works with the tools your team already uses."
5. **(section)** `#demo` — H: "Ready to get your team aligned?"

<!-- AUTO:END -->

## Nx mapping (manual)

**Route:** `/focus-alignment` · **Page:** `src/pages/landing/LandingFocusAlignment.tsx` · **CSS:** `src/styles/landing-focus-alignment.css`

| HTML section | Nx component (`…/components/landing/pages/focus-alignment/…`) | Notes |
|--------------|--------------------------------------------------------------|-------|
| 1. hero | `FocusHero.tsx` | |
| 2. "AI that understands your context" | `FocusContextChat.tsx` | |
| 3. "Built for the whole team…" | `FocusTeamSection.tsx` | |
| 4. "Works with the tools…" | `FocusIntegrations.tsx` | |
| 5. `#demo` "Ready to get your team aligned?" | `FocusDemoBand.tsx` | custom demo band, **not** the shared `LeadSection` |

## Design intent & interactions (manual)

- Custom `#demo` band (inline-styled) instead of standard lead form. ~8 keyframes, one `IntersectionObserver`. Context-chat section is the visual centerpiece.
