# salesx.html — UI Spec

<!-- AUTO:START — regenerated by `npm run spec`. Do not edit below. -->

### Tokens (overrides vs `_tokens.md`)

- `--navy`: #001F5C
- `--navy-2`: #001340
- `--navy-3`: #000d2e
- `--blue`: #1863DC
- `--blue-soft`: #EEF3FF
- `--blue-mid`: #C7D4FA
- `--amber`: #1863DC
- `--paper`: #FFFFFF
- `--bg`: #f9fafb
- `--ink`: #1a1f2e
- `--muted`: #64748b
- `--line`: #e5e7eb
- `--line-dk`: rgba(255,255,255,.12)
- `--accent-light`: rgba(0,31,92,.07)
- `--fg`: #1a1f2e
- `--fg-secondary`: #334155
- `--font-display`: "Be Vietnam Pro",system-ui,sans-serif
- `--font-body`: "Be Vietnam Pro",system-ui,sans-serif
- `--section-gap`: clamp(48px,6vw,72px)

### Section inventory

1. **numbers**
2. **how** — H: "Recover 20–30% of lost leads. Handle 10x the inquiries. Without adding headcount." · assets: https://images.unsplash.com/photo-1600596542815-ffad4c1539a9?w=600&q=80, https://images.unsplash.com/photo-1512917774080-9991f1c4c750?w=120&q=70, https://images.unsplash.com/photo-1502005229762-cf1b2da7c5d6?w=120&q=70, https://images.unsplash.com/photo-1560185007-c5ca9d2c014d?w=120&q=70
3. **(section)** — H: "What happens when AI SalesX is not there."
4. **(section)** — H: "Win the listing before you sell it."
5. **lead-section** `#demo` — H: "Book a demo and watch your job get done by SalesX"

<!-- AUTO:END -->

## Nx mapping (manual)

**Route:** `/salesx` · **Page:** `src/pages/landing/LandingSalesx.tsx` · **CSS:** `src/styles/landing-salesx.css`

| HTML section | Nx component (`…/components/landing/pages/salesx/…`) | Notes |
|--------------|------------------------------------------------------|-------|
| (hero — not a top-level `<section>`) | `SalesxHero.tsx` | HTML hero isn't a `<section>`, so extractor didn't list it; still present visually |
| 1. numbers | `SalesxNumbers.tsx` | |
| 2. how | `SalesxSurfaces.tsx` + `SalesxFeatures.tsx` + `SalesxCalendar.tsx` | Nx splits this into 3; in HTML they're nested inside `.how`, not separate top-level sections |
| 3. "What happens when AI SalesX is not there" | `SalesxDifference.tsx` | |
| 4. "Win the listing before you sell it" | `SalesxWinListing.tsx` | |
| 5. lead-section `#demo` | `sections/LeadSection.tsx` | shared (copy: "…get done by SalesX") |

_Granularity gap: HTML has 5 top-level sections, Nx has 7 section components — several Nx sections (Surfaces/Features/Calendar) are nested `<div>`s inside HTML `.how`. Verify boundaries in nx. `SalesxChatMockup.tsx` is an embedded sub-component._

## Design intent & interactions (manual)

- Most token overrides of any page (23) — bespoke dark/light palette (`--ink`, `--line-dk`). Uses external Unsplash imagery (see AUTO assets). ~6 keyframes, one `IntersectionObserver`.
